class Solution:
    def shuffle(self, nums: List[int], n: int) -> List[int]:
        new=[]
        i=0
        j=n
        while j<len(nums):
            new.append(nums[i])
            i=i+1
            new.append(nums[j])
            j=j+1
        return new    
