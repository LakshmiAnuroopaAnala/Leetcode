class Solution {
public:
    vector<int> sortedSquares(vector<int>& nums) {
        vector<int> r;
        for(int i=0;i<nums.size();i++)
        {
            r.push_back(nums[i]*nums[i]);
        }
        sort(r.begin(), r.end());
        return r;
    }
};
